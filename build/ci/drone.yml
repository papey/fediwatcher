global-variables:
  RUST_VERSION: &RUST_VERSION 1.36.0

kind: pipeline
name: main

steps:
  - name: fmt
    image: rust:$${RUST_VERSION}
    commands:
      - rustup component add rustfmt --toolchain $${RUST_VERSION}-x86_64-unknown-linux-gnu
      - cargo fmt -- --check
